/*
 Cards.createCardStore = function() {
  var cards = [];
  var eventEmitter = new EventEmitter();

  var addCard = function( card ){
    cards = cards.concat(card);
    EventEmitter.trigger("change",me);
  };

  var getCards = function(){
    return cards;
  };

  var onChange = function(){
  }
  
  var me = {
    addCard: addCard,
    getCards: getCards
  };

  return me;
};
*/
